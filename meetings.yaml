# 미팅 목록에 쓰이는 날짜·장소.
#
# 본문은 조직위원회가 Google Sites에서 관리하므로 여기서 건드리지 않는다.
# 이 파일은 목록 화면과 '다가오는 일정'에 쓸 날짜·장소만 담는다.
# 값이 없으면 제목에서 연·월만 뽑아 쓴다(예: "Oct 2026 SPARCS XIV" → 2026.10).
#
# ⚠️ 아래 두 건은 원문을 직접 확인한 값이다.
#    나머지 지난 모임의 날짜·장소는 아직 채우지 않았다 —
#    원문에서 확인한 뒤 같은 형식으로 추가할 것.

meetings:
  /meetings/oct-2026-sparcs-xiv:
    when: "2026.10.19–23"
    where: "서울 MICE Plaza"

  /meetings/aug-2026-ska-korea-summer-workshop:
    when: "2026.08.19–21"
    where: "대전 KAIST 자연과학대학 E6-1"
