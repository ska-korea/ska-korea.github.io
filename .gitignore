# 빌드 산출물 — Actions가 매번 새로 만든다
_site/

# 미팅 원고 — 별도 저장소 ska-korea/meetings의 체크아웃 (CI가 받아온다.
# 로컬에서는 git clone git@github.com:ska-korea/meetings.git meetings-src)
meetings-src/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# macOS · Dropbox
.DS_Store
*conflicted copy*
.dropbox*

# 편집기
.idea/
.vscode/
*.swp
