# 빌드
Jinja2>=3.1
Markdown>=3.5
PyYAML>=6.0

# 수확 (Google Sites에서 미팅 본문 가져오기)
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
# 미팅 배너를 화면에서 쓰는 폭으로 줄여 담는다(원본은 7000px·2.5MB짜리도 있다).
# 없으면 원본 그대로 저장하고 수확은 계속된다.
Pillow>=10.0
